import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.bonadio.pokeapi',
  appName: 'pokeApi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
